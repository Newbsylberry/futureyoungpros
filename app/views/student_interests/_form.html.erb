<div class="row">
  <div class="col-xs-12 col-md-6">
    <%= form_for(@student_interest) do |f| %>
    <% if @student_interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_interest.errors.count, "error") %> prohibited this student_interest from being saved:</h2>
      <ul>
	<% @student_interest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
	<% end %>
      </ul>
    </div>
    <% end %>

    <div class="form-group">
      <%= f.label :firstName, "First Name:" %><br>
      <%= f.text_field :firstName, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :lastName, "Last Name:" %><br>
      <%= f.text_field :lastName, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :school_id, "School:" %><br>
      <%= f.text_field :school_id, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= label :category_id,"Category:" %><br>
      <%= collection_select(:mou, :category_id, Category.all, :id, :name) %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
</div>
<% end %>

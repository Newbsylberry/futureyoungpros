<%= provide(:title, 'Businesses') %>
<%= render 'layouts/staticbanner' %>
<div class="well">
  <h2>Businesses</h2>
  <p class="lead">Apply to a Junior Achievement Job Site!</p>
</div>
<div class="row">
  <div class="col-sm-5 col-lg-5 col-md-offset-1">
    
    <% @job_sites.each do |job_site| %>
        <h1> <%= job_site.name %></h1>
        <%= link_to "Now Hiring Junior Achievers!", job_site %>
    <% end %>
    
  </div>
  <div class="col-sm-5 col-lg-5">
    
      <%= image_tag("#{@pagename}1.jpg", :width => "100%", :height => "100%") %>
    
  </div>
  <div class="col-sm-1 col-lg-1">
  </div>
</div>
